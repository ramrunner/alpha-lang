#ifndef __GENERAL_HEADER
#define __GENERAL_HEADER

#include <string>
#include <vector>
#include <utility>
#include <stack>
#include <iostream>
#include <fstream>
#include <map>
#include <sstream>
#include <cstring>

using namespace std;

#define max(a,b)	(a>b) ? (a) : (b)
#define min(a,b)	(a<b) ? (a) : (b)

extern ostream *cdeb;

#endif
